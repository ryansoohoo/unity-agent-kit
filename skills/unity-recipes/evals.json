{
  "skill": "unity-recipes",
  "queries": [
    { "query": "im using sleep 5 after i edit a script to wait for the compile, is that ok", "expect": "unity-recipes", "note": "recipe 1 positive: names the canonical bad pattern; register: junior, typo" },
    { "query": "whats the right way to read the unity console without burning 20k tokens", "expect": "unity-recipes", "note": "recipe 2 positive (console read); register: ryan" },
    { "query": "i wrote a bunch of files into Assets/ from a script, do i just wait for unity to notice them", "expect": "unity-recipes", "note": "recipe 3 positive (refresh after writing assets); register: junior" },
    { "query": "show me the good and bad way to do a perf investigation in unity", "expect": "unity-recipes", "note": "recipe 4 positive, asks for the pairing explicitly; register: neutral" },
    { "query": "our agent keeps pasting the entire Editor.log into chat, whats the better habit", "expect": "unity-recipes", "note": "recipe 2 positive in another console habit (Editor.log, not the console window); register: other-console" },
    { "query": "what should an agent do right after writing an asset file so unity picks it up", "expect": "unity-recipes", "note": "recipe 3 positive phrased as agent procedure; register: neutral" },
    { "query": "give me the dos and donts for driving unity from the command line", "expect": "unity-recipes", "note": "skill-level positive: the bad/good pairing is the whole ask; register: casual" },
    { "query": "is it bad practice to spam a console read every second while im working", "expect": "unity-recipes", "note": "recipe 2 positive framed as an anti-pattern check; register: junior, typo" },
    { "query": "i keep getting the same warning 50 times when i read the log, how do people filter that", "expect": "unity-recipes", "note": "recipe 2 positive (dedup, errors-only); register: casual" },
    { "query": "check my script against the known unity agent anti-patterns before i hand it back", "expect": "unity-recipes", "note": "positive: the pre-handback diff re-read that unity-verify points here for; register: ryan" },
    { "query": "just tell me if MyScript.cs compiled, cheapest check you've got", "expect": "unity-verify", "note": "near-miss (verify boundary): compile vocabulary, but the ask is a verdict not a pattern; register: casual" },
    { "query": "two agents both want to trigger a refresh at the same time, who gets the editor", "expect": "unity-topology", "note": "near-miss (topology boundary): refresh keyword, ownership question; register: casual" },
    { "query": "console is clean but git left Level1.unity as UU after the pull", "expect": "unity-merge", "note": "near-miss (merge boundary): console vocabulary as a distractor; register: casual" },
    { "query": "can you write these bad and good patterns into our CLAUDE.md so the juniors follow them", "expect": "unity-claude-md", "note": "near-miss (claude-md boundary): this skill's own vocabulary, but the ask is instruction text; register: casual" },
    { "query": "make a mac build for the artists to try", "expect": "none", "note": "polysemy: player build is unclaimed territory; register: casual" },
    { "query": "our nightly build machine has to spit out an exe every night", "expect": "none", "note": "polysemy: build-server/exe phrasing; register: casual" },
    { "query": "how do i make a custom editor window with a button in it", "expect": "none", "note": "Unity how-to sharing the editor keyword; authoring editor UI is claimed by no kit skill; register: junior" },
    { "query": "whats the best way to read a csv at runtime", "expect": "none", "note": "shares the read keyword with recipe 2 but is ordinary gameplay code; register: junior" },
    { "query": "after the refresh, how do i confirm the new type is attachable", "expect": "unity-verify", "note": "near-miss: recipe 3 vocabulary handing off to the tier-2 verification it ends in; register: neutral" },
    { "query": "the artist says our fbx assets import at the wrong scale, whats going on", "expect": "none", "note": "import vocabulary on an asset-pipeline question no kit skill claims; register: casual" }
  ]
}
